{
  "name": "parcel-jest-base",
  "version": "1.0.0",
  "description": "npm install",
  "default": "index.html",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "test": "jest --watch --env='jsdom'",
    "test-once": "jest --env='jsdom' --coverage",
    "coverage": "istanbul cover ./src/Tarea-logica-negocios.test.js"
  },
  "author": "Israel Antezana R.",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.15.4",
    "@babel/preset-env": "^7.15.4",
    "babel-jest": "^27.1.0",
    "istanbul": "^0.4.5",
    "jest": "^27.3.1",
    "parcel": "^2.0.0-rc.0",
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "jsdom": "^18.0.1"
  }
}
